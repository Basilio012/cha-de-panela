import React from 'react';
import ReactDOM from 'react-dom/client';
import ChaDePanel from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ChaDePanel />
  </React.StrictMode>
);
